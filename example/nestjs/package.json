{
  "name": "rvlog-nestjs-example",
  "private": true,
  "scripts": {
    "predev": "pnpm -C ../.. build && pnpm -C ../../packages/rvlog-nest build",
    "dev": "nest start --watch",
    "prestart": "pnpm -C ../.. build && pnpm -C ../../packages/rvlog-nest build",
    "start": "nest start"
  },
  "dependencies": {
    "@kangjuhyup/rvlog": "workspace:*",
    "@kangjuhyup/rvlog-nest": "workspace:*",
    "@nestjs/common": "^11.1.0",
    "@nestjs/core": "^11.1.0",
    "@nestjs/platform-express": "^11.1.0",
    "reflect-metadata": "^0.2.2"
  },
  "devDependencies": {
    "@nestjs/cli": "^11.0.10",
    "typescript": "^5.7.0"
  }
}
